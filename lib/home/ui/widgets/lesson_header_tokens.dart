/// Design tokens for LessonHeader to avoid magic numbers in the widget.
class LessonHeaderTokens {
  // Padding around the whole header
  static const double horizontalPadding = 16.0;
  // increase vertical padding slightly so header feels taller
  static const double verticalPadding = 10.0;

  // Sizes
  // Make flag/icon bigger so header is visually closer to bottom nav height
  static const double flagSize = 44.0;
  static const double iconSize = 32.0;

  // Spacing
  static const double iconTextSpacing = 8.0;

  // Typography
  static const double valueFontSize = 18.0;
  static const String valueFontFamily = 'DuolingoFeather';
}
